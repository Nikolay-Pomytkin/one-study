<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
        <a href="/" class="navbar-brand">
          <span class="glyphicon glyphicon-home"></span> One-Study
        </a>
    </div>
    <div class="navbar-right">
      <% if user_signed_in? %>
        <p class="navbar-text">Signed in as <b><%= current_user.name %></b></p>
        <%= link_to "Manage Account", edit_user_registration_path, class: "btn btn-default navbar-btn navbar-margins" %>
        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-default navbar-btn navbar-margins" %>
      <% else %>
        <%= link_to "Log In", new_user_session_path, class: "btn btn-default navbar-btn navbar-margins" %> 
        <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-default navbar-btn navbar-margins" %>
      <% end %>
    </div>
  </div>
</nav>